import { styled } from '@mui/material';

export const CheckboxWrap = styled('div')(() => ({
  display: 'flex',
  alignItems: 'center',
}));

export const checkboxLabel = { inputProps: { 'aria-label': 'CreateCv' } };
